workflows:
  default-workflow:
    name: Default Workflow
    instance_type: linux
    environment:
      groups:
        - keystore_credentials
    scripts:
      - name: Build APK
        script: |
          flutter build apk --release
    artifacts:
      - build/**/outputs/**/*.apk
      
